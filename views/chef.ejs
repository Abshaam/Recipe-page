<html lang="en">
    <!-- head here -->
    <%- include('./partials/head') %>
<body>
    <!-- nav here -->
  <%- include('./partials/nav') %>

  
      <div class="chef-container">
        <!-- <div class="chef">
            <img src="./images/Chef.jpg" alt="">   -->
        
        <% if (signUps.length > 0) { %>
            <% signUps.forEach(post => { %>
                  
                <div class="chef-card"> 
                    <a href="/get/<%= post._id %> "> 
            <img class="cards-img" src="/upload/profileImage<%= post.profileImage %> " alt="pic">
          </a>
          <h2><%= post.name %> </h2>
          <p><%= post.address %> </p>
          <p><%= post.email %> </p>
          
          </div>
          
            <% }) %>
            <% }  else { %>  
                <p><%= "No posts to display currently" %> </p>
          
                <% } %>  %>
          
      </div>
 

  

  <!-- footer here -->
  <%- include('./partials/footer') %>